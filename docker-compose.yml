version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_PATH=/data/jupiter_fresh.db
      - UPLOAD_DIR=/data/uploads
    volumes:
      - /var/jupiter_fresh_data:/data
    restart: unless-stopped
